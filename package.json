{
  "name": "sem",
  "version": "1.0.0",
  "description": "Automated semantic versioning and release pipeline — a full semantic-release clone written in Tish",
  "main": "src/main.tish",
  "bin": {
    "sem": "src/main.tish"
  },
  "scripts": {
    "start": "tish run --feature full src/main.tish",
    "release": "tish run --feature full src/main.tish",
    "release:dry": "tish run --feature full src/main.tish --dry-run",
    "test": "tish run --feature fs,process,regex test/semver.test.tish && tish run --feature fs,process,regex test/commit-parser.test.tish && tish run --feature fs,process,regex test/commit-analyzer.test.tish && tish run --feature fs,process,regex test/release-notes.test.tish",
    "test:integration": "tish run --feature full test/integration.test.tish",
    "test:all": "npm test && npm run test:integration"
  },
  "keywords": [
    "semantic-release",
    "release",
    "semver",
    "conventional-commits",
    "changelog",
    "automation",
    "tish"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "sem": {
    "branches": ["main"],
    "plugins": [
      "@sem/commit-analyzer",
      "@sem/release-notes-generator",
      "@sem/changelog",
      "@sem/npm",
      "@sem/github",
      "@sem/git"
    ]
  }
}
